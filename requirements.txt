# Python dependencies for the Kidney scRNA-seq ML project.
#
# CPU-only and reproducible; no GPU/cuML/Colab required. Versions are pinned to
# what the project was developed and last run against.
#
# Install with:  pip install -r requirements.txt
# (or `pip install -e .` to install the kidney_scrna package itself)

# --- Core pipeline (notebooks + package) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
imbalanced-learn>=0.11        # SMOTE + RandomUnderSampler (balancing inside CV folds)
scikit-optimize>=0.9          # BayesSearchCV (SVM hyperparameter search)

# --- EDA / notebook plots ---
matplotlib>=3.7
seaborn>=0.12

# --- Serving layer (train.py + FastAPI API + demo) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
joblib>=1.3

# --- ONNX export (export_onnx.py: model.joblib -> model.onnx) ---
skl2onnx>=1.16
onnx>=1.15
onnxruntime>=1.17            # also the runtime the deployment serves
